# Bun config.
# Strict peer dependency posture carried over from the previous .npmrc.

[install]
# Fail installs on peer-dependency conflicts instead of silently papering over —
# conflicts you don't see at install time are conflicts you hit in prod.
exact = false
peer = true
