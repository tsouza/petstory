# Interaction flow specs

One file per implemented user story: `US-<CAT>-<NNN>.md`.

Written just-in-time when the PR implementing the story lands. Captures the screen-by-screen interaction the story's JTBD format deliberately omits (WR5).

Convention, template, and the "when to add a Mermaid diagram" rule live in [`../guidelines.md`](../guidelines.md) → WR9.

This directory is empty until the first story ships.
