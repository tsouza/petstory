{
  "name": "@petstory/test-utils",
  "version": "0.0.0",
  "private": true,
  "description": "Shared test helpers — fixtures, MSW handlers, Flow DSL test harness. Consumed via workspace:* by every package's test suite.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "biome check . --max-diagnostics 0"
  },
  "devDependencies": {
    "@petstory/config": "workspace:*"
  }
}
