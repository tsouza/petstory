{
  "name": "@petstory/ui",
  "version": "0.0.0",
  "private": true,
  "description": "L3 primitives — brand-aware, pack-neutral components, tokens, icons. NativeWind-based per ADR-006.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "biome check . --max-diagnostics 0"
  },
  "devDependencies": {
    "@petstory/config": "workspace:*"
  }
}
